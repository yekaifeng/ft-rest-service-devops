apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ft-rest-service
  labels:
    app: ft-rest-service
spec:
  selector:
    matchLabels:
      app: ft-rest-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
      scheme: http
