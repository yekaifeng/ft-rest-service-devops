apiVersion: v1
kind: Service
metadata:
  name: ft-rest-service
  labels:
    app: ft-rest-service
spec:
  selector:
    app: ft-rest-service
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
